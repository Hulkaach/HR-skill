server:
  port: 8090

spring:
  datasource:
    url: "jdbc:postgresql://localhost:5433/postgres"
    username: postgres
    password: new_passwd2
    driver-class-name: org.postgresql.Driver
  flyway:
    enable: true
    locations: classpath:db/migration
    schemas: public